[project]
name = "moma-screenshots"
version = "0.1.0"
description = "Real-terminal screenshot generator for the moma CLI"
requires-python = ">=3.10"
dependencies = [
    "pillow>=10.0",
    "pyte>=0.8",
    "pyyaml>=6.0.3",
]

[tool.uv]
package = false
